{
  "_comment": "Provenance for the vendored Form.io type snapshot in component-types.ts. The step-03 diff job compares KNOWN_FORMIO_COMPONENT_TYPES against upstream @formio/core at the ref below and alerts on any new builtin type. Bump `ref` + `capturedFrom` when the snapshot is refreshed; never hand-edit component-types.ts to silence an alarm.",
  "package": "@formio/core",
  "ref": "master",
  "capturedFrom": "src/base/**/index.ts and src/components/**/*.ts (builtin component `type` registrations)",
  "note": "Content-free: this file lists type identifiers only, never any user form content."
}
