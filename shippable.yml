language: node_js

before_install:
  - cd ./src/dxSurvey/
  - npm install -g gulp
  - npm install -g bower
  - npm install -g bower-installer
  - npm install -g typescript
  - bower-installer
 
script:
  - gulp makedist
  - gulp test_ci