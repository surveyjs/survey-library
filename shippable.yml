language: node_js

before_install:
  - cd ./src/dxSurvey/
  - npm install -g gulp
  - npm install -g bower
  - npm install -g bower-installer
  - npm install -g typescript
  - bower-installer
 
script:
  - gulp typescript
  - gulp sass
  - gulp templates
  - gulp test_ci