language: node_js

before_install:
  - npm install -g gulp
  - npm install -g bower
  - npm install -g bower-installer
  - npm install -g typescript
  - bower-installer
 
script:
  - gulp copylibs
  - gulp makedist
  - gulp test_ci