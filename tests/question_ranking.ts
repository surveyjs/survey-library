import { QuestionRankingModel } from "../src/question_ranking";

export default QUnit.module("question ranking");

QUnit.test("Ranking: ", function (assert) {
  assert.equal(true, true, "Ranking is ok");
});
